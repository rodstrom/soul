using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace Soul.Manager
{
    /*class MenuManagerOptions : MenuManager
    {

        List<Widget> widgetList = null;

        public MenuManagerOptions(InputManager controls)
            : base(controls)
        {
            widgetList = new List<Widget>();
        }

        public void AddButton(ImageButton button, Widget widget)
        {
            AddButton(button);
            if (widget != null)
            {
                widgetList.Add(widget);
            }
        }
    }*/
}
